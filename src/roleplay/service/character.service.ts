import { Injectable, Logger } from "@nestjs/common";@Injectable()

/**
 * Character Service defines the bot dynamic persona and behavior in roleplay scenarios.
 * Aim is to create an utility AI that can tries to fulfill the chat goals
 * while maintaining an engaging and coherent character.
 */
export class CharacterService {
  private logger = new Logger("Roleplay/CharacterService");


}
