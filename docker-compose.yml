version: '3.8'
services:
  app:
    build: .
    depends_on:
      - redis
    environment:
      - REDIS_URL=redis://redis:6379
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - MESSAGE_LIMIT=${MESSAGE_LIMIT}
  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"

