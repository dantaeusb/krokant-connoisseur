version: '3.8'
services:
  app:
    build: .
    depends_on:
      - mongo
    environment:
      - MONGO_URL=mongodb://mongo:27017/bot
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - MESSAGE_LIMIT=${MESSAGE_LIMIT}
  mongo:
    image: "mongo:8.0"
    ports:
      - "27017:27017"
